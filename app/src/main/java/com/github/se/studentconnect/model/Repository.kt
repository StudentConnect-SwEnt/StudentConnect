package com.github.se.studentconnect.model

/**
 * Base interface for all repositories.
 *
 * All repositories must inherit from this and must be defined as Kotlin `object` so they are
 * guaranteed to be singletons.
 */
interface Repository

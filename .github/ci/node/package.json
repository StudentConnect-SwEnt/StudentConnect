{
  "name": "studentconnect-ci-tools",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "firebase-tools": "^14.0.0",
    "wait-on": "^9.0.0"
  },
  "scripts": {
    "start-firebase-emulators": "firebase emulators:start --only auth,firestore,storage --project demo-project",
    "wait-for-firebase": "wait-on tcp:8080 tcp:9099 tcp:9199"
  }
}

